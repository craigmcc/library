// constants.ts

/**
 * Manifest constants shared across the application.
 *
 * @packageDocumentation
 */

// LocalStorage keys ---------------------------------------------------------

/**
 * Storage key for an array of Library objects.
 */
// Keys to local storage stored data
export const LIBRARIES_KEY = "LIBRARIES";

/**
 * Storage key for the currently selected Library object.
 */
export const LIBRARY_KEY = "LIBRARY";

/**
 * Storage key for login data for the currently logged-in User.
 */
export const LOGIN_DATA_KEY = "LOGIN_DATA";

/**
 * Storage key for the User object of the currently logged-in User.
 */
export const LOGIN_USER_KEY = "LOGIN_USER";

